import { beforeAll, afterAll } from 'vitest';
// import { setupDatabase, teardownDatabase } from './helpers/test-db';

beforeAll(async () => {
  // Global setup like connecting to a test database if needed
  // await setupDatabase();
});

afterAll(async () => {
  // Global teardown
  // await teardownDatabase();
});
